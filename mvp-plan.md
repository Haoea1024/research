# 文献研读 Agent — 初步阶段（MVP）开发计划

> **2026-09 阅读器形态修订**：默认阅读体验调整为“左侧原始 PDF + 右侧保留论文版式骨架的中文版式页”；现有结构化 Block 流保留为辅助/调试视图。仍坚持**不重建、不写回译文 PDF**，右侧使用 HTML/CSS 根据 `page + bbox + order_idx + type` 复刻页面结构，并允许中文局部 reflow。Figure/Table 在正文中原位保留，AI 图表卡片改为按需展开的详情层。后续 S5/S6 的所有锚点统一回跳该版式 Reader。

> 完整设计见 [paper-reading-agent-design.md](paper-reading-agent-design.md)。本文只定义初步阶段范围。
> 已结合 GitHub 同类项目调研修订（pdf2zh/BabelDOC、paper-qa/kotaemon/ragflow、ChatPaper/gpt_academic、MinerU/marker/docling、Zotero 生态），关键借鉴与教训见第八节。

**MVP 定义**：上传 PDF → **保留原论文版式骨架的中英对照阅读** → 能看图的基础问答 → 按需生成的**完整八段精读报告（含三层改进方向生成器）**。初步阶段唯一不做的大块是组会工作台。

## 一、MVP 功能清单（做到什么程度）

1. **PDF 解析流水线**：MinerU 单选型，**锁定 3.x + VLM 后端，requirements 锁死 mineru/torch/vllm 精确版本组合**（社区实录：torch 版本或 MinerU 升级都能打崩阅读顺序，spike 通过的版本号写进文档）。代码上定义统一 `Parser → List[Block]` 接口，MinerU 是唯一实现（不写第二实现，但留降级位；降级候选为 Qwen3-VL 系 API 直出带 data-bbox 的 HTML，失败模式与版面模型路线正交且零 GPU）。输出按阅读顺序的内容块，含 page+bbox、公式转 LaTeX、图片裁切；MinerU 的 `discarded_blocks`（页眉/页脚/页码）直接跳过不入翻译流。公式识别加**字体名正则（CM*/Ital 等数学字体）+ 运算符字符正则**兜底过滤层，拦截漏检的行内公式被翻成乱码。模型权重下载做成安装脚本内带镜像源的显式步骤。解析子进程设超时与内存上限，失败显式落状态。数据模型四张表：Paper / Block / Translation / Report（Block 含 `confidence` 字段，MinerU 有则存、无则空，低置信块 UI 弱提示），SQLite + 本地文件。
2. **左右对照阅读器（默认版式对照 + 结构化辅助视图）**：左侧继续由 pdf.js 原样渲染 PDF；右侧默认不再使用“卡片式翻译块流”作为主阅读界面，而是渲染**中文版式页**。中文版式页以解析得到的 `page + bbox + order_idx + type` 为版式骨架：保留页级结构、单双栏关系、块的横向位置/宽度、标题层级以及 Figure/Table 的原位关系；中文段落**不强行塞回原 bbox 高度**，允许按列局部增高与后续块顺次下移（local reflow），避免字号缩小、重叠和溢出。该视图是 HTML/CSS 重排，不生成或写回译文 PDF。现有结构化 Block 流完整保留为 `structured` 辅助/调试模式，用于查看块状态、重译、错误与锚点；默认 `viewMode=layout`，可切换 `layout | structured`。同步滚动与悬停互指继续以共享 `block.id + page + bbox` 为唯一身份链：左侧当前主块驱动右侧对应中文块定位，反向亦然；目标 PDF 页未渲染时以 `RenderTask.promise` 的 page-ready 状态等待后再绘制 bbox，不用固定延时。块级操作 MVP 只保留“重译”；Figure/Table 在中文版式页中原位展示裁切原图/解析表格和翻译图注，AI 图表解释通过点击后的详情层展示，不挤占论文正文排版。
3. **分块翻译（完整做，不打折）**：首轮以 Paper/Run 级共享步骤生成一次术语表 → 携带术语表+前后文的分块翻译 → 持久化缓存 → SSE 流式逐块返回。**术语表从第一天就是 `source,target` 结构化表（可导出 CSV）**；同一 Paper 用 per-paper lock + 二次查询保证并发 Run 只产生一次 glossary 业务调用，glossary 失败则整 Run fail-fast，不把未翻译 Block 误写成 failed。翻译调度用**视口优先的可插队队列**：优先当前视口及下一屏，用户跳页不必等前面翻完；接口带 `pages`/`block_ids` 范围参数。入队前确定性跳过空白、URL/邮箱、纯 HTML、arXiv/脚注元数据等不可翻译内容；batch 采用 **per-block validation / partial commit**，单个无效输出不拖累同批有效译文。缓存写入前做语言漂移校验，URL/邮箱/HTML 噪声不计入中文比例。`done` 和 `failed` 都是普通 submit 的持久终态；只有用户显式 `retranslate` 才允许 failed 再次产生收费调用。重译只刷新该 Block，不更新论文级术语表。块级幂等缓存，部分完成是合法持久状态，中断后继续未完成工作。
4. **图表卡片（轻量版，详情层而非排版层）**：解析完成后，Figure/Table 的 parser-derived 数据（裁切图、caption、page/bbox、MinerU 表格结构）先组成可用 Card；视觉模型只负责补充 `vision_description` / 视觉转录等 **vision-derived** 字段，不能覆盖或伪装成 parser 原始结果。真实视觉调用前先用 fake pipeline 验证状态、缓存与 provenance；真实模型另行 gate。视觉 prompt 强制注入图注原文 + 图所在页前后正文段落。表格保留 MinerU 原始 `table_html`，并做确定性的 HTML→cell JSON；未来视觉表格转录写入独立字段，不覆盖 parser 结果。Card 进入向量库供检索，但在 Reader 主体中只保留原位 Figure/Table + 中文图注；AI 描述、关键趋势、结构化信息通过点击后的 drawer/popover 展开。不做正文引用关系自动分析。
5. **基础 RAG 问答（多模态）**：文本块 + 图表卡片两路入 sqlite-vec，不做 BM25 混检 / rerank / 意图判别 / 多轮指代。上下文固定携带摘要+术语表；检索命中图表卡片时把裁切原图一并交给多模态模型作答。**锚点采用"受限候选集选 id"方案**：prompt 给编号候选块，结构化输出中锚点字段只允许取值自候选集，代码侧校验非法 id 丢弃/重试（kotaemon 的"LLM 复述原文短语→前端模糊匹配"被其自身 issue 证明失败率极高，不走该路线）。回答带块级来源锚点可点击跳转；点击后统一调用 Reader 的 anchor 导航：左侧 PDF 滚到对应页/bbox，右侧中文版式页滚到对应中文块，并在两侧同时高亮。**证据可信度信号 + 拒答出口**：检索分数全低时侧栏显式警告"本回答依据不足"；prompt 明确允许"论文未提供该信息"（PaperQA2 约 22% 的主动拒答率正是其准确率口碑来源）。
6. **完整八段精读报告（用户触发 + 对话感知）**：①任务设定、②研究动机、③核心 Insight、④完整方法流程、⑤关键公式直观解释（符号表→直观类比→去掉这一项会怎样）、⑥实验结果与消融分析（内含**"复现风险审查"小节**，按固定 checklist 审读：benchmark 选择是否恰当 / 有无数据泄漏嫌疑 / 指标是否误用 / 是否 post-hoc 挑结果——给"哪些实验设置值得怀疑"一个有文献依据的骨架，借鉴 Hidden Pitfalls 研究）、⑦优缺点与新颖性评价、⑧可改进方向。④⑥ 必须结合图表卡片讲解（相关图表卡片+裁切原图交给多模态模型，讲解引用具体图表并带图表锚点）。**段间上下文链**：生成后段时携带已生成段的摘要，prompt 显式要求"不要重复前面 section 内容""数值必须用原文数字""专有名词保留英文"（防八段冗余与术语漂移）。不自动生成——用户读完/问完后点击"生成精读报告"，可勾选"结合我的提问记录"：把本篇问答摘要作为上下文，报告优先展开用户关注和困惑过的点。**约束：对话只影响侧重点，所有事实必须重新锚定原文，不采信此前对话中的结论**。各段同样允许"论文未提供该信息"。按 section 懒生成、入库，每条论断带锚点（同样走受限候选集方案）。
7. **改进方向三层生成器 + 具体性检查器**（融入 auto research 领域的可靠机制，全部为 prompt 层，零训练）：第一层沿"假设/模块/实验设定"三轴枚举可改动点，枚举后用有效性/可行性/新颖性三个评审角色各给一句批评再修订一轮（ResearchAgent 多维自反馈模式；**用 checklist 不打分**）；第二层产出 2-3 张研究提案卡（可证伪的研究问题 + 最小验证实验：数据集/baseline/指标/预期区间，"一周单卡可跑出信号"级具体度 + 风险与 Plan B + **可行性接地字段**：数据集是否公开/预估算力与时长/最可能失败点——斯坦福人评实验发现可行性是 LLM idea 的最大短板 + **信息缺口清单**：论文未给出的关键超参/细节及建议默认值，与拒答机制同构，借鉴 SciReplicate）；第三层对每张提案卡给论文故事线（标题候选×3 / Abstract 五句话 / Intro 段落大纲 / 实验章节设计 / 审稿人质疑预判），**故事线生成 3 个候选后 LLM 两两比较取胜者**（pairwise 选优替代单次生成，LLM 直接打分不可靠是该领域核心教训）。**反空泛条款前置进 ⑦⑧ 生成 prompt**（"禁止不带具体论据的 novelty 泛评""每条 weakness/改进点必须编号且点名具体模块/实验设定"），检查器（规则 + 轻量 LLM：不含具体数据集/模块/指标名的建议自动打回重生成）作为兜底——前置约束降低重生成率、省 Opus 费用。默认不注入用户研究背景（不局限已有设定），注入为可选项。
8. **划选提问**：可在左侧原 PDF 的已识别块或右侧中文版式页的对应块发起提问，底层都转换为稳定 `block_id`；选中块直接塞入上下文（不走检索），选中 Figure/Table 时附原图。成本极低且是精读场景提问主入口。
9. **单篇论文管理与工程纪律**：列表 + 解析/翻译状态。FastAPI BackgroundTasks，无任务队列，无用户系统。**每个 LLM/解析任务显式三态 UI（loading/失败/重试）+ 超时（如 60s），失败信息带原始错误码；key/base_url 在首次调用前校验并给诊断信息**（同类项目"永远卡在 generating"和"部署即劝退"是 issue 区最大公约数）。**所有 LLM 结构化输出（锚点/术语表/提案卡/检查器）走严格 Pydantic 校验 → 失败带错误信息重试 1 次 → 仍失败降级展示并标注"未能锚定"，绝不静默吞掉**（kotaemon 高亮失灵的直接原因就是校验失败静默丢弃）——这条写进 `llm.py` 封装层职责。

## 二、明确不做（推迟）

| 功能 | 理由 |
|---|---|
| 图表-正文引用关系自动分析 | 精读结合图表靠"把全部图表卡片给模型自选相关图"实现即可；引用关系图谱是优化项 |
| 组会工作台全部（对比矩阵/脉络/讲稿/PPT/演练/Cheat Sheet） | 低频场景，完全是单篇精读产出的组合消费 |
| 问答意图判别路由、BM25 混检、rerank 模型、多轮指代、联网搜索 | 先验证纯向量 RAG 能否命中正确块；若命中率不达标，**第一升级选项是 Haiku 批量相关性打分（paper-qa RCS 的降级版：召回 top-10 后一次调用批量打分过阈值再进 Sonnet）**，优先级高于 BM25/rerank |
| 术语表反馈更新 UI、领域预置术语库 | 存储已是结构化 CSV（第 3 条），加编辑 UI/预置层零迁移，等真实需求 |
| 块手动合并/拆分、坏块软删除标记 | bbox 调试页（第六节 R1）加个按钮即可实现软删除，等观察到真实错误模式再上 |
| 解析质量报告、公式点击展开解释、纯译文全宽/交替显示等额外视图 | 默认 `layout` 版式对照与 `structured` 辅助视图已纳入 MVP；其它显示模式继续后置 |
| 八段生成后的"整合润色"pass | 若验收发现八段割裂再加（gpt_academic 两阶段模式） |
| Qwen3-VL 降级解析器实现、Postgres、Qdrant、任务队列 | 接口位已留（第 1 条）；单机 SQLite + sqlite-vec + 后台线程足够 |
| 用户研究背景注入、Zotero 集成 | 个性化与分发渠道后置（Zotero 生态热度证明它是 MVP 后最值得考虑的方向） |

## 三、实现顺序（按依赖，不设时间周期）

```
S0 解析验证 spike（go/no-go 门，先于一切产品代码）      ← 详见第六节 R1
S1 地基：解析流水线（Parser 接口 + MinerU 实现）
        + 四表数据模型 + llm.py 封装层（含路由配置、结构化输出校验、三态/超时）
S2 对照阅读器骨架：pdf.js 原文 + 右侧结构化英文 Block 流 + 双向同步
S3 翻译流水线：术语表(CSV)→分块→缓存→SSE，视口优先队列，
   pages 参数，语言漂移校验；接入阅读器 + 悬停互指 + 单块重译  ← 里程碑：翻译链可用
S3.5 版式翻译 Reader：右侧默认切换为“中文版式页”，
     由 bbox/页/列关系复刻版式骨架 + 中文局部 reflow；保留 structured 视图
     + Figure/Table 原位展示 + 共享 anchor 导航                    ← 里程碑：像论文一样读
S4A 本地 embedding 入库 + sqlite-vec + parser-derived Figure/Table Card
    + parser_table_json + 安全图片端点 + fake vision pipeline
S4B 真实视觉模型验收：Figure/Table vision description / vision table data
    + Card 缓存/版本/失败重试；Card 作为详情层，不替代 Reader 原位图表
S5 RAG 问答侧栏（两路检索、看图作答、受限候选集锚点、
   可信度信号与拒答、统一 anchor 回跳版式 Reader）+ 划选提问  ← 里程碑：能问
   （锚点可靠性验证 R2 在此顺带完成，先跑通精读 ③ 一段）
S6 精读八段全量（段间上下文链，④⑥ 结合图表）
   + 对话感知 + 改进方向三层 + 具体性检查器；报告锚点回跳版式 Reader ← 里程碑：能精读
S7 回归验收：用 5 篇本方向真实论文全流程走查 + prompt 打磨
```

范围收缩时的可砍项（按序）：① 报告的对话感知（退化为仅论文上下文）② 具体性检查器（退回人工验收）。八段与三层是核心，不可砍；图表问答不可砍。

## 四、模型选型与可替换性

**可替换性设计（第一天就落地，成本极低）**：
1. **业务代码不写模型名**。所有 LLM 调用走统一封装层 `llm.py`，业务侧只声明任务名：`llm.call("translate", ...)`、`llm.call("figure_card", images=...)`。
2. **任务→模型路由表放配置文件**（`config.yaml`），改配置即换模型，不改代码：
   ```yaml
   providers:
     anthropic: { api_key_env: ANTHROPIC_API_KEY, base_url: https://api.anthropic.com }  # base_url 可指向中转
   tasks:
     translate:    { provider: anthropic, model: claude-haiku-4-5, max_tokens: 4000 }
     figure_card:  { provider: anthropic, model: claude-sonnet-5 }
     qa:           { provider: anthropic, model: claude-sonnet-5 }
     report_insight: { provider: anthropic, model: claude-opus-5 }
   ```
3. **Key 只从环境变量读**（`.env`，不入库不入代码仓），`base_url` 可配置以支持中转站/代理。
4. **底层客户端用 LiteLLM**（或自写 provider 适配接口）：天然兼容 Anthropic/OpenAI/Gemini 等多家，后期换厂商只改配置里的 provider+model 两个字段。注意 prompt caching 是 Anthropic 特性，封装层要做能力探测、不支持时自动降级为普通调用。LiteLLM 自带成本追踪，config 层顺手打开。
5. **缓存结果带模型版本戳**：Translation / Figure / Report 表都加 `model` 字段。换模型后旧结果不自动失效（还能读），但 UI 可显示"由旧模型生成"，用户可选择性重刷；embedding 同理加 `embedding_model` 字段，换 embedding 模型必须整库重建索引。

**当前默认路由**：

| 任务 | 模型 | 理由 |
|---|---|---|
| 分块翻译（量最大） | claude-haiku-4-5 | 便宜快速，带术语表+上下文窗口后翻译质量足够；单篇成本压到最低 |
| 术语表生成（每篇一次） | claude-sonnet-5 | 需要通读定调，量小，值得用中档 |
| 图表卡片（视觉描述+表格转录，每图/表一次） | claude-sonnet-5 | 多模态，性价比高；每篇约 10-20 次调用 |
| RAG 问答(含看图作答) | claude-sonnet-5 | 日常主力，多模态；配合 prompt caching(论文全文做缓存前缀)压增量成本 |
| 精读 ③核心 Insight、⑦优缺点新颖性、⑧改进方向三层（可改动点/提案卡/故事线） | claude-opus-5 | 最吃深度推理和"不空泛"的环节，量小（每篇每 section 一次且缓存），用最强档 |
| 精读 ①②④⑤⑥（结构化讲解段，④⑥ 多模态看图） | claude-sonnet-5 | 结构化拆解为主，Sonnet 足够且支持视觉 |
| 具体性检查器 | 规则 + claude-haiku-4-5 | 轻量校验：检查建议是否含具体数据集/模块/指标名 |
| Embedding | bge-m3（本地）或 Voyage voyage-3 | Anthropic 无 embedding API；bge-m3 免费、中英跨语检索强（中文提问查英文原文块），单机可跑；不想自托管则用 Voyage |

单篇论文首次处理成本估算：翻译（Haiku）+ 图表卡片与术语表（Sonnet）约 $0.1-0.3；精读八段全量按需生成，③⑦⑧三层（Opus）+ ①②④⑤⑥（Sonnet）约 $0.8-1.5；问答靠 prompt caching 后每轮增量很小。全部结果持久化，同一篇只花一次。

## 五、下一阶段预告（组会工作台 + auto research 借鉴项）

初步阶段验收通过后，下一阶段做组会工作台（原设计 M3）：5 篇论文的对比矩阵与演进脉络、共同盲区分析、按时长/听众生成讲稿 + Marp PPT、三人格模拟提问演练 + Cheat Sheet。它的输入完全是本阶段的精读报告，所以本阶段精读质量打磨到位是前提。详见完整设计文档第 7 节。

结合 auto research 领域调研，下一阶段同期加入（按收益/成本排序）：
1. **提案卡新颖性查重闭环**（收益/成本比最高）：提案核心 claim → 生成 Semantic Scholar 检索式（语义+关键词两路，纯关键词会漏语义近邻）→ top-k 近似论文 → LLM 判相似 → 输出"与工作 X 的差异声明"或触发重写（CoI/SciMON 已验证该闭环；S2 API 免费）。直接回答组会最致命的问题"这不是 XX 做过了吗"。UI 措辞用"未检索到高度相似工作，最相近的是 X，差异在 Y"，不说"确认新颖"（检索有假阴性）。
2. **对比矩阵证据锚定 + 盲区扫描**：每格强制原文引用+页码/图号（bbox 现成），无证据显示"未提及"；"共同盲区"实现为"某维度 5 篇全空"的矩阵扫描（Elicit 高准确率的关键就是证据锚定）。
3. **演进脉络用链式结构 + 大纲先行**：S2 引文关系 + LLM 判"谁改进谁"组成链/分叉树，链尾外推趋势；先出可编辑的演进大纲再逐节填证据，避免浅罗列（CoI 验证链式显著优于平铺）。检索域默认锁定 5 篇论文的索引，外部检索仅限一跳补锚点论文（如共同引用的开山作）且需用户确认——不做开放式联网综述，避免把引用幻觉引入干净链路。
4. **模拟提问改多轮追问式**：三人格各基于 RAG 做 2-3 轮追问式模拟对话再沉淀问题（STORM 视角引导模式），加入复现性质询模板（"baseline 重跑过吗""这是 post-hoc 挑的吗"）。
5. **可行性三档分级器**：提案卡附件，仿 CORE-Bench：L1 原论文有代码+数据 / L2 有代码缺数据 / L3 需从零实现，附预估工作量（查 GitHub/PapersWithCode 链接+规则即可）。
6. **引用可验证性后置校验器**：生成物锚点二次核验（span 是否真在解析结果中、是否支撑论断），失败标黄——受限候选集之上的纵深防御第二层。
7. **历史产出记忆库**：精读报告、提案卡入向量库，新论文分析时检索历史产出（"这个盲区在 X 论文也出现过"）——跨论文盲区本就需要此基础设施。

明确不做（边界固化进设计原则）：自动执行实验/沙箱复现（最好系统成功率仅 21-40%，且"错误产物看起来和正确产物一样"对信任是毁灭性的）；部署/微调评审大模型（与零 GPU 架构冲突，改用真实评审意见数据集做 few-shot 提升"评审腔"）；实验代码骨架生成降格为远期（先有规格与缺口清单，骨架若做也定位"2 小时能跑起来的起点"并明示不承诺正确，永不接执行沙箱）。

## 六、必须先验证的技术风险

**R1（最高）：MinerU 对复杂版式的阅读顺序、bbox 精度与图-图注配对**。同步滚动、悬停互指、翻译分块、**中文版式页的列归属/跨栏判断**、问答/精读锚点全部建立在“块划分正确 + bbox 准确”之上；图表卡片、多模态问答、精读④⑥ 三条链路的共同地基是图裁切与图注配对——第三方实测这恰是 MinerU 的最弱环节。
**验证**（写任何前端之前）：取 10 篇代表性论文（本方向 5 + NeurIPS/ACL 双栏 3 + 公式密集 2）跑纯解析脚本，写一个把 bbox 画回 pdf.js 并标块序号的可视化页面（**此页面转正为常驻调试入口，不当一次性脚本扔掉**），量化六项：① 块阅读顺序正确率（每篇抽查 20 块）② bbox 与段落视觉重合度 ③ 公式/图注类型识别率 ④ **图-图注配对与裁切完整性（单列过线 ≥85%）** ⑤ 公式转 LaTeX 可用率 ⑥ **版式骨架恢复可用性**（单双栏归属、跨栏标题/图表判断不会把阅读顺序拆乱；用于 S3.5 的确定性布局推导）。**通过线：顺序 ≥95%、bbox 可用 ≥90%、图-图注 ≥85%**。验收脚本写成断言式（"块 A 必须在块 B 前""该图注必须配对该图"），S7 回归免费复用（olmOCR-Bench 方法论）。spike 顺带用 1-2 篇跑 Qwen3-VL API 对比留底。
不达标处置（按序）：公式类问题先加字体/字符正则兜底层 → 评估 Qwen3-VL API 降级 → 仍不达标则降级为 section 级对齐或把"手动块修正"提回 MVP。此决策必须发生在前端动工前。

**R2：锚点式生成的可靠性**。精读/问答的论断能否稳定引用正确 block id（幻觉锚点 = 违背"锚定原文"承诺）。
**验证**（S5 期间，半天）：直接从"受限候选集选 id"方案起步（同类项目已证明"复述原文再匹配"路线失败率极高，不再作为首选尝试），对 3 篇已解析论文人工核查 30 条论断的锚点命中率，**正确拒答（"论文未提供该信息"）计为正确**；<80% 则收紧候选集大小与校验重试策略。

## 七、MVP 验收标准

1. 任选一篇 NeurIPS 双栏论文：上传后 10 分钟内可开始对照阅读；左侧原 PDF 保真，右侧默认中文版式页能保持页级结构、单双栏关系、标题层级以及 Figure/Table 的对应位置，中文允许局部 reflow 但不得出现重叠/蚂蚁字/跨栏串行；同步滚动与悬停互指不漂移。结构化 Block 视图可切换且与默认版式视图共享同一 `block.id`。页眉页脚页码不进入翻译主体。
2. 翻译术语全文一致（抽查 5 个核心术语），术语表可导出 CSV；刷新页面翻译秒开（缓存生效）；跳到未翻译页时该页译文优先出现（视口优先调度生效）；无整块英文残留（语言漂移校验生效）。
3. 问答：10 个关于该论文的问题，≥8 个回答正确（**正确拒答计为正确**）且来源锚点可点击跳转到正确位置（左 PDF bbox + 右中文版式对应块同时定位/高亮）；其中至少 3 个是必须看图/表才能答的问题（如"主实验里哪个方法最好、好多少""Figure 2 的趋势说明什么"），≥2 个答对并给出图表锚点，涉及表格数值的问题能命中具体数字（表格转录生效）。
4. 精读八段均可生成，论断锚点抽查正确率 ≥80%，无静默丢锚点（失败块显式标注"未能锚定"）；报告锚点点击后回跳版式 Reader 并同步定位原 PDF；④⑥ 的讲解中引用了具体图表且图表锚点可跳转；⑤ 对每条核心公式给出符号表和直观类比；八段间无明显内容重复（段间上下文链生效）。
5. 改进方向三层齐备：可改动点全部落到具体假设/模块/实验设定名；≥2 张提案卡的最小验证实验具体到数据集+baseline+指标（"一周单卡可跑出信号"级）；每张提案卡有完整故事线（标题候选/Abstract/Intro 大纲/审稿人预判）。全程无"更多数据/更大模型"式空话（具体性检查器拦截率抽查）。
6. 勾选"结合提问记录"生成的报告至少覆盖用户问过的 2 个主题，且其中事实均有原文锚点。
7. 健壮性：任一 LLM 任务超时/失败时 UI 显式报错可重试，不出现"永远转圈"；解析中断后重跑从已完成块续起。
8. 你本人连续 3 天用它读真实论文，不需要回退到裸 PDF；并用 1 篇论文的三层改进方向输出，自评"是否真的启发了可执行的新 idea"。

## 八、同类项目调研结论备忘（设计决策依据）

1. **差异化确认**：GitHub 无任何项目同时做到"块级 bbox 锚点 + 八段结构化精读 + 三层改进方向生成"。ChatPaper 是固定三段总结（无锚点不看图）、gpt_academic 精读是 4 个固定问题、paper-qa 是纯问答库、kotaemon 引用高亮走不可靠的文本匹配。“pdf.js 原文 + bbox 驱动的中文版式复刻 + block 级双向互指”在开源界是空白——也意味着阅读器与锚点链路没有现成代码可抄。
2. **架构决策记录：不做"重建译文 PDF"**（pdf2zh/BabelDOC 路线）。其 issue 高频区——字体崩溃、译文溢出文本框、装饰线错位、表格乱码、扫描件重影、非 PDF/A 兼容——全部来自"写回 PDF"这一步；pdf2zh 1.x 作者本人承认因此难以维护而重写。我们“左侧 pdf.js 原样渲染不动原 PDF + 右侧 HTML/CSS 中文版式页（保留版式骨架、允许局部 reflow）”天然绕开整类坑。评审时不要被"人家能输出双语 PDF"带偏。
3. **锚点决策记录：不走"LLM 复述原文短语→前端模糊匹配"**（kotaemon 路线，其 issue 实证弱模型下十次不到一次成功），也不走"答案内联 `##i$$` 标记"（ragflow 路线，前端解析 bug 成灾）。结构化字段 + 受限候选集 + 严格校验是唯一可靠形态。
4. **解析选型记录**：2026 年 MinerU 在"稳定块级 bbox + 阅读顺序 + 图表裁切"的工程化输出上仍综合最优（docling 双栏阅读顺序是重灾区；marker 权重许可有商用门槛且小字幻觉与 MinerU 同源；纯 VLM 小模型 OCR 精度已反超但无我们要的稳定块级输出）。风险集中在图-图注配对与版本回归两点，已分别用 R1 指标④ 和锁版本对冲。
