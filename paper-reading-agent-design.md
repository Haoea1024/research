# 文献研读 Agent 设计方案

> **2026-09 阅读器形态修订**：单篇阅读的默认界面改为“左侧原始 PDF + 右侧中文版式页”。右侧不是译文 PDF，也不是卡片流，而是由解析块的 `page/bbox/order/type` 驱动的 HTML/CSS 版式复刻；保留页面/单双栏/图表/标题关系，中文允许局部 reflow。现有结构化 Block 视图保留为辅助模式。Figure/Table 的 AI Card 是点击后的详情层；S5/S6 的所有来源锚点统一回跳该 Reader。

> 一个面向科研人员的英文文献精读助手：保留论文版式骨架的中英对照阅读、结构化精读引导、基于文献的智能问答、组会多篇论文分享工作台。

---

## 1. 产品定位与用户场景

**目标用户**：需要精读英文论文的研究生 / 科研人员。

**核心场景**：
1. **单篇精读**：上传 PDF → 左原 PDF / 右中文版式对照阅读 → 系统引导式精读 → 随时提问 → 沉淀笔记与精读报告。
2. **组会准备**：围绕 5 篇论文，完成对比分析 → 生成分享讲稿与 PPT 大纲 → 模拟提问演练。

**设计原则**：
- 所有 AI 产出必须**锚定原文**（可点击跳转到 PDF 对应位置），杜绝无出处的泛泛而谈；锚点一律走"结构化字段 + 受限候选集 + 严格校验"，不走"复述原文再匹配"（kotaemon 路线已被其自身 issue 证伪）；
- 精读内容面向"能启发新研究"而非"读懂就行"，改进方向必须给出可执行的实验方案；
- 图表是一等公民：解析、讲解、问答都要能引用论文中的图表；
- **不重建译文 PDF，但要保留版式骨架**：pdf2zh/BabelDOC 的字体崩溃、译文溢出、线条错位、表格乱码等高频 issue 全部来自“写回 PDF”这一步。左侧原样渲染；右侧用 HTML/CSS 根据 `page+bbox+order+type` 复刻页面结构、单双栏与图表位置，并允许中文局部 reflow。目标是“看起来仍是同一篇论文的中文版”，而不是像素级替换原 PDF；
- **生成与设计可自动，执行与验证归人**（auto research 调研结论）：系统产出研究规格、查重证据、可行性分级，但不自动执行实验、不宣称实验结果、不对无检索证据的新颖性下结论——auto research 系统的可靠区是发散生成/结构化互评/检索接地，不可靠区是实验自动执行与结果自评（最好的端到端系统复现成功率仅 21-40%），我们只吸收前者；
- **LLM 排序一律 pairwise 或 checklist，禁止直接打 1-10 分**（斯坦福大规模人评实验结论：LLM 打分不可靠、成对比较才有信号），写入 llm.py 封装层约定。

---

## 2. 总体架构

```
┌─────────────────────────────── 前端 (React) ───────────────────────────────┐
│  版式对照阅读器        精读面板           问答侧栏          组会工作台          │
│ (原 PDF + 中文版式页) (结构化报告)       (RAG 对话)        (对比矩阵/讲稿/演练) │
└──────────────────────────────────┬─────────────────────────────────────────┘
                                   │ REST / SSE (流式输出)
┌──────────────────────────────────┴─────────────────────────────────────────┐
│                          后端 (FastAPI + 任务队列)                           │
│                                                                             │
│  ① 解析流水线      ② 翻译流水线      ③ 精读 Agent      ④ 问答 Agent          │
│  MinerU/marker    分块翻译+术语表    多阶段生成         意图判别+RAG          │
│                                                                             │
│  ⑤ 组会 Agent：跨论文对比 / 讲稿生成 / 模拟提问                               │
└──────────────────────────────────┬─────────────────────────────────────────┘
                                   │
        ┌──────────────┬───────────┴────────┬──────────────────┐
        │ 对象存储      │ 结构化存储          │ 向量库            │
        │ PDF/图片      │ SQLite/Postgres    │ 分块embedding     │
        │              │ (块/翻译/笔记/报告)  │ (文本+图表描述)    │
        └──────────────┴────────────────────┴──────────────────┘
                        LLM 层：Claude API（文本 + 视觉多模态）
```

---

## 3. 模块一：PDF 解析流水线（一切能力的地基）

对照翻译、精读、问答的质量上限都取决于解析质量，这一步值得重点投入。

**技术选型**：**MinerU**（或 marker 作备选）做版面分析，输出：
- 按阅读顺序排列的**内容块**（block）：段落 / 标题 / 公式 / 图 / 表 / 图注 / 引用；
- 每个块的**页码 + 坐标 bbox**（对照高亮和跳转全靠它）；
- 公式转 LaTeX，表格转 HTML/Markdown，图片裁切为独立文件；
- GROBID 补充提取元数据（标题、作者、venue、参考文献列表）。

**图表理解预处理**（关键增强）：解析完成后，先用 parser-derived 数据形成可用 Figure/Table Card（裁切图、caption、page/bbox、表格原始结构）；视觉模型只补充 vision-derived 字段。Card 是检索/详情层数据，不替代 Reader 中原位 Figure/Table。真实视觉调用生成的内容必须与 MinerU 原始字段分开保存：
```json
{
  "figure_id": "fig3",
  "caption": "原始图注",
  "vision_description": "该图展示了……横轴是……可以看出方法A在……时显著优于……",
  "nearby_block_ids": ["b41", "b42"],        // 确定性上下文；正文引用关系自动分析后置
  "page": 5, "bbox": [...],
  "provenance": {"caption": "parser", "vision_description": "vision"}
}
```
图表卡片同时进入向量库，使精读和问答都能“看图说话”；Reader 正文只展示原位图片/表格、图注及必要状态，AI 描述按需展开。

**数据模型（核心表）**：
```
Paper(id, title, authors, venue, year, pdf_path, status)
Block(id, paper_id, page, bbox, type, order, content_md, latex?)
Translation(block_id, zh_text, glossary_version)
Figure(id, paper_id, caption, image_path, vision_description, bbox)
Note(id, paper_id, block_id?, content, created_at)
Report(paper_id, section, content_md, anchors[])       // 精读报告，含原文锚点
Workspace(id, name, paper_ids[])                        // 组会工作台
```

---

## 4. 模块二：左右对照阅读（保留版式骨架）

**核心决策：原 PDF 不动，中文版式页也不写回 PDF。** 左侧用 pdf.js 原样渲染 PDF（公式、图表、字体和原始排版 100% 保真）；右侧默认渲染一份 HTML/CSS **中文版式页**。中文版式页以解析结果中的 `page + bbox + order + type` 为几何骨架，优先保留：

- 页级边界与页序；
- 单栏/双栏关系、跨栏标题；
- Block 的横向位置与宽度比例；
- 标题层级和正文顺序；
- Figure/Table 与正文的相对位置；
- 公式、图注、表格的原始角色。

**不追求像素级复刻。** 中文与英文长度不同，右侧不能把译文强行塞进原 bbox 高度。实现上使用“横向位置尽量保持 + 纵向局部 reflow”：同一列中的中文 Block 可按内容自然增高，后续 Block 顺次下移；跨栏图表/标题作为独立 layout group。宁可页面局部变长，也不通过极端缩小字号来强行一页一像素对应。

### 4.1 两种阅读视图

默认提供：

1. **版式对照（`layout`，默认）**：左原 PDF + 右中文版式页，是日常精读主界面；
2. **结构化（`structured`）**：保留现有 Block 流，显示 block id/type/status、译文、skip/error、重译等，作为调试、精确操作和数据核验视图。

两种视图共享同一个 `block.id` 身份链，不复制业务状态。未来“纯译文全宽/交替显示”等属于额外 view mode，不影响底层数据。

### 4.2 版式推导

布局数据由解析结果**确定性派生**，不调用 LLM：

```text
page
bbox
order_idx
type
→ column / layout_group / x_ratio / width_ratio / span
```

`bbox` 的横向信息主要用于列归属、横向位置和宽度；纵向 `y` 主要作为排序/间距提示，不作为中文固定高度。遇到结构不确定时优先保持阅读顺序与可读性，不伪造精确坐标。

### 4.3 交互设计

- **双向同步**：以主 `block.id` 为同步单位。左侧滚动确定当前主块 → 右侧定位对应中文块；右侧滚动反向定位原 PDF 页+bbox。中文 reflow 后不依赖两侧像素滚动比例。
- **悬停互指**：悬停原 PDF bbox、中文版式 Block 或结构化 Block 时，其他视图共享高亮。
- **统一锚点导航**：问答、精读报告、图表详情中的来源锚点都调用同一 `navigateToAnchor`，同时定位左 PDF 与右中文版式块。
- **块级操作**：重译、提问等操作仍绑定 `block.id`；结构化视图提供更完整的状态/错误信息。
- **图表处理**：中文版式页在对应布局位置保留原 Figure crop / parser Table，并显示翻译后的 caption。AI `FigureCard/TableCard` 不作为大卡片插进正文，而是点击图表后在 drawer/popover 中展示 vision description、关键趋势、结构化数据和相关正文。
- **公式处理**：公式不翻译，右侧在对应位置用 KaTeX 渲染原 LaTeX；未来的直观解释按需展开，不改变正文排版。

**翻译流水线**：
1. 首轮快速通读全文，生成**术语表**（term → 领域标准中文译法），存库供全文复用，保证 "attention head" 全文译法一致；
2. 按 block 分批翻译，携带术语表 + 前后文窗口，学术语体，保留专有名词英文原文（首次出现给“中文（English）”格式）；
3. 翻译结果缓存入库，流式返回；右侧中文版式页只订阅 Translation 状态，不改变翻译业务语义；
4. 用户可对任一块显式“重译”；重译只刷新该 Block 的 Translation，不自动修改论文级术语表。

---

## 5. 模块三：结构化精读引导（核心差异化能力）

### 5.1 精读报告的固定框架

精读不是一次性长文生成，而是**多阶段流水线**，每个 section 独立生成、独立可刷新，且每条论断都带 `[原文锚点]`（点击后同时跳到左侧 PDF 对应 bbox 与右侧中文版式块）：

| Section | 内容要求 |
|---|---|
| ① 任务设定 | 输入/输出形式化定义、问题边界、与相邻任务的区别 |
| ② 研究动机 | 现有方法的具体缺陷（引用原文 related work / intro 的证据）、为什么此时做这个问题 |
| ③ 核心 Insight | 用 1-3 句话提炼"这篇论文真正的那一个想法"，并回答"为什么这个想法能解决动机中的缺陷" |
| ④ 完整方法流程 | 分步骤拆解 pipeline，**结合方法架构图讲解**（引用图表卡片），标注每步的输入输出维度 |
| ⑤ 关键公式直观解释 | 每条核心公式给出：符号表 → 直观类比 → 与代码实现的对应 → "如果去掉这一项会怎样" |
| ⑥ 实验与消融 | 主实验结论、**每张实验图表的解读**、消融实验揭示了哪个模块真正 work、哪些实验设置值得怀疑 |
| ⑦ 优缺点与新颖性 | 相对已有工作的真实增量、局限性（作者承认的 + 作者没说的）、新颖性评级与理由 |
| ⑧ 可改进方向 ★ | 详见 5.2 |

### 5.2 改进方向生成器（重点设计，拒绝泛泛而谈）

这是你明确要求的重点。设计一个**三层递进**的生成结构，并用 prompt 约束强制具体化：

**第一层：可改动点枚举**。系统沿三个轴系统性扫描：
- **假设轴**：论文的每条显式/隐式假设（如"训练测试同分布"、"标签干净"、"单模态输入"），逐条问"放宽/去掉这条假设后问题还成立吗？会变成什么新问题？"
- **模块轴**：方法流程中的每个模块，逐个问"这个模块的替代方案是什么？消融显示它贡献多大？换掉它的风险和收益？"
- **实验设定轴**：数据集规模/领域、评测指标、baseline 选择、算力设定，逐项问"换一个设定，结论还成立吗？哪个设定最可能翻车？"

**第二层：新研究问题形成**。从第一层中选 2-3 个最有潜力的改动点，每个产出一张**研究提案卡**：
```
研究问题：一句话（必须是可证伪的 claim，不是方向性口号）
为什么现在可做：依赖的技术/数据/算力条件
预期贡献：如果成立，对领域意味着什么
最小验证实验 (MVP)：数据集 + baseline + 指标 + 预期结果区间，
                     要求"一周内单卡可跑出信号"级别的具体度
风险与备选：实验不 work 的最可能原因 → Plan B
```

**第三层：论文故事线组织**。对每张提案卡给出成文骨架：
- 标题候选 ×3；
- Abstract 五句话模板（背景→缺陷→我们的洞察→方法→结果）；
- Intro 段落级大纲（每段的论证职责）；
- 实验章节设计：主表 + 必要消融 + 分析实验各证明什么 claim；
- 预判审稿人的 3 个最可能质疑及回应策略。

**Prompt 约束**（写进系统提示词）：禁止出现"可以尝试更多数据集/更大模型/结合最新技术"这类无信息量表述；每个建议必须落到具体的数据集名、模块名、指标名；必须与本论文的消融证据或局限性形成逻辑链。

### 5.3 交互形态

- 精读报告在右侧独立面板呈现，各 section 手风琴式展开，按需生成（省 token）；报告由用户主动触发（通常在阅读+问答之后），而非上传即自动生成；
- **对话感知生成**：触发时可勾选"结合我的提问记录"——将本篇问答摘要作为上下文，报告优先展开用户关注和困惑过的点，并标注"与你之前的提问相关"。约束：对话只影响侧重点，所有事实必须重新锚定原文，不采信此前对话中的结论；
- 支持**深度追问**：每个 section 下有"再深入"按钮，如公式解释可追问"逐符号推导"；
- 支持**用户研究背景注入**（可选）：用户可在设置里填写自己的研究方向，改进方向生成时同时给出"通用改进"和"与你方向结合"两组——但默认不受限于用户已有设定（呼应你的要求）。

---

## 6. 模块四：文献问答（带相关性判别）

**两级路由**：

```
用户提问 → [意图判别器：轻量 LLM 调用]
  ├─ A. 与当前论文相关 → RAG：检索文本块 + 图表卡片 + 精读报告片段
  │      → 回答中每个论断标注 [来源锚点]，点击统一回跳版式 Reader（左 PDF bbox + 右中文版式块）
  ├─ B. 领域相关但超出本文 → 明确告知"论文未涉及"，
  │      基于模型知识回答并清晰区分"论文说的" vs "我补充的"，可选联网搜索
  └─ C. 无关闲聊 → 正常回答，不检索
```

**检索设计**：
- 混合检索：向量（embedding）+ BM25 关键词，对公式/专有名词类问题 BM25 很重要；
- 检索单元 = 文本块 + 图表卡片 + 精读报告 section，三路召回后 rerank；
- 问答上下文中始终携带论文元数据 + 摘要 + 术语表，保证小问题不用检索也答得准；
- 多轮对话保留历史，支持"上一个问题里你说的 X 是指什么"这类指代。

**加分设计**：用户可在左侧原 PDF 的已识别块或右侧中文版式块上直接“就此提问”——两侧都映射到同一 `block.id`，该块自动作为最高优先级上下文。

---

## 7. 模块五：组会分享工作台（针对"5 篇论文分享"场景）

这是为你的组会场景专门设计的模块，核心思想：**组会分享的本质不是 5 篇论文的摘要串烧，而是讲出一条"研究脉络" + 经得起提问**。

### 7.1 工作台与阅读管理

- 新建工作台"X月X日组会"，拖入 5 篇论文；
- 看板显示每篇的状态：已解析 / 已翻译 / 精读完成度 / 我的笔记数；
- 一键批量触发解析 + 翻译 + 精读，后台队列跑，完成后通知。

### 7.2 跨论文对比矩阵（自动生成，可编辑）

| 维度 | Paper 1 | Paper 2 | ... |
|---|---|---|---|
| 任务设定 | | | |
| 核心 insight | | | |
| 方法关键差异 | | | |
| 数据集与指标 | | | |
| 主要结果 | | | |
| 局限性 | | | |

每个单元格内容来自各论文的精读报告（复用，不重复生成），带锚点可跳回原文。矩阵下方自动生成**关系分析**：
- 5 篇论文的**演进脉络图**（谁改进了谁、谁和谁是并行竞争路线、共同依赖什么前提）；
- **共同盲区**：5 篇都没解决的问题——这往往就是你下一个研究方向，也是组会上最出彩的观点。

### 7.3 分享讲稿与 PPT 生成

- 输入：分享时长（如 30 分钟）、听众背景（同方向/跨方向）、叙事策略；
- **叙事策略可选**：① 时间演进线（A 提出问题 → B 改进 → C 换思路…）② 问题拆解线（大问题的 5 个子方面）③ 对比批判线（两大技术路线之争）——系统根据 5 篇论文的实际关系推荐最合适的一种；
- 输出①：**逐页 PPT 大纲**（Marp Markdown，可直接导出 PPTX/PDF），自动嵌入各论文的关键图表（解析阶段已裁好图）；
- 输出②：**逐页讲稿**，标注每页的建议用时和"这页要传达的一句话"；
- 结尾页自动生成"我的观点"：基于共同盲区分析给出你自己的评论和潜在方向——组会分享有无自己观点是拉开差距的关键。

### 7.4 模拟提问演练（Rehearsal 模式）

- 系统扮演三种提问者人格：**较真的导师**（追问技术细节和公式）、**跨方向同学**（问动机和大图景）、**审稿人视角**（挑实验漏洞）；
- 基于 5 篇论文的精读报告生成 15-20 个高概率被问的问题，附参考答案与出处锚点；
- 交互演练：你口头/文字作答，系统对照论文内容指出答错、答漏之处；
- 生成**Cheat Sheet**：一页纸的关键数字（各论文的核心指标、数据集规模、参数量），组会现场被问细节时救命用。

---

## 8. 技术选型汇总

| 层 | 选型 | 理由 |
|---|---|---|
| PDF 解析 | MinerU（备选 marker）+ GROBID | 版面分析+公式+表格+阅读顺序，开源可控 |
| 前端 | React + pdf.js + KaTeX + Tailwind | 左侧 pdf.js 保证原文 100% 保真；右侧 HTML/CSS 版式页支持中文 local reflow 与 Block 级交互 |
| 后端 | FastAPI + SSE 流式 + arq/celery 任务队列 | 解析和批量翻译是长任务，必须异步 |
| 存储 | SQLite（单机起步）→ Postgres；本地文件存 PDF/图 | 起步轻量 |
| 向量库 | sqlite-vec 或 Qdrant | 单篇论文块数不大，起步不需要重型方案 |
| LLM | Claude API 分级：翻译/意图判别 claude-haiku-4-5；术语表/图表卡片/问答/多数精读 section claude-sonnet-5（多模态）；核心 Insight、改进方向三层、讲稿生成 claude-opus-5 | 按任务难度分级，控成本；量大的用小模型，吃深度推理的用大模型 |
| Embedding | bge-m3（本地自托管）或 Voyage voyage-3 | Anthropic 无 embedding API；bge-m3 中英跨语检索强且免费 |
| 模型接入层 | LiteLLM + 任务→模型路由配置（config.yaml）+ key 走环境变量、base_url 可配 | 业务代码只写任务名不写模型名：换 key、换中转、换厂商、换模型都只改配置；缓存结果带 model 版本戳支持选择性重刷 | 
| PPT 导出 | Marp（Markdown → PPTX/PDF） | 生成物可编辑、可版本化 |

**成本与性能策略**：
- 翻译、图表卡片、精读报告全部**持久化缓存**，同一论文只算一次；
- 精读报告按 section 懒生成；
- 长文档用 prompt caching（论文全文作为缓存前缀，多次问答只付增量）；
- 单篇论文预估：解析免费（本地）+ 翻译约 3-8 万 token + 精读约 5-10 万 token，主要成本在首次处理。

---

## 9. 开发路线（两个阶段）

**阶段一 — 初步阶段/MVP（详见 mvp-plan.md）**：解析验证 spike → 解析流水线 → 对照阅读器骨架 → 分块翻译含术语表 → **S3.5 中文版式 Reader** → S4A 本地 embedding + parser Figure/Table Card → S4B 真实视觉 Card → 多模态 RAG 问答（含来源锚点、划选提问）→ 完整八段精读报告（④⑥结合图表、用户触发、对话感知）→ 改进方向三层生成器 + 具体性检查器。
*验收：默认阅读模式能保持论文页/列/图表结构且中文局部 reflow 无重叠；双向同步不漂移；问答能看图且锚点可同时定位原 PDF 与中文版式；精读八段全量可用，改进方向通过“具体到数据集/模块/指标”检查。*

**阶段二 — 能分享**：组会工作台 → 对比矩阵与脉络分析 → 共同盲区 → 讲稿 + Marp PPT 生成 → 模拟提问演练 + Cheat Sheet。
*验收：用你下次组会的真实 5 篇论文跑通全流程，产出可直接使用的 PPT 初稿。*

---

## 10. 风险与对策

| 风险 | 对策 |
|---|---|
| PDF 解析质量参差（扫描版、双栏复杂版式） | MinerU 失败时降级 marker/PyMuPDF；提供"解析质量报告"提示用户哪些页可能有问题 |
| 翻译块与原文对齐漂移 | 身份对齐完全基于稳定 `block.id + page + bbox`，不做文本模糊匹配；中文版式只改变展示布局，不改变 Block 身份 |
| 中文版式溢出/重叠 | 不做像素级 bbox 替换；横向位置/列宽尽量保留，纵向允许局部 reflow，宁可局部页高变化也不把中文压成小字 |
| 精读产出幻觉（编造实验数字） | 强制锚点机制：无锚点的论断在 UI 中标灰并提示"未在原文找到依据"；数字类内容生成后做原文比对校验 |
| 改进方向仍然空泛 | 生成后跑一个"具体性检查器"（规则+LLM）：不含具体数据集/模块/指标名的建议打回重生成 |
| 首次处理等待久 | 上传后立即可读原文；翻译流式逐块出现；精读后台跑完通知 |
